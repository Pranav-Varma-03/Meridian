# Use official npm registry
registry=https://registry.npmjs.org/

# Use strict SSL
strict-ssl=true

# Hoist all dependencies for better compatibility
shamefully-hoist=true

# Store pnpm cache locally in the project
store-dir=.pnpm-store
