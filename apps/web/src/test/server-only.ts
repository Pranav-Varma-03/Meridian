// Vitest executes server modules in a Node test environment. Next.js enforces the
// production server/client boundary; this empty module lets focused server unit tests
// load those modules without changing production behavior.
export {};
