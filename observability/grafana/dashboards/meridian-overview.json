{
  "annotations": {"list": [{"builtIn": 1, "type": "dashboard"}]},
  "editable": true,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum(rate(meridian_http_requests_total[5m]))", "refId": "A"}],
      "title": "API request rate",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "ms"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(meridian_http_server_duration_ms_bucket[5m])))", "refId": "A"}],
      "title": "API p95 latency",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "sum by (stage, outcome) (rate(meridian_worker_jobs_total[5m]))", "refId": "A"}],
      "title": "Worker outcomes and queue health",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 4,
      "targets": [{"expr": "sum by (dependency, outcome) (rate(meridian_dependency_operations_total[5m]))", "refId": "A"}],
      "title": "Dependency outcomes",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 16},
      "id": 5,
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le, stage) (rate(meridian_rag_stage_duration_ms_bucket[5m])))", "refId": "A"}],
      "title": "RAG stage p95 latency",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["meridian", "operational"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Meridian operational overview",
  "uid": "meridian-overview",
  "version": 1
}
